# 永久属性加成服务端开发文档

## 一、模块架构

### 1.1 模块概述

永久属性加成模块（ForeverAdd Module）负责管理玩家的永久属性加成数据，包括：
- 永久加成的获取与消耗
- 玩家属性加成计算
- 加成数量限制管理

### 1.2 组件类型

组件类型: `ENPPlayerCompType.FOREVER_ADD`

---

## 二、核心数据结构

### 2.1 永久加成组件 (ForeverAddComponent)

**路径**: `NPUSServer/NPUSUserMgr/UserComp/ForeverAddComp/ForeverAddComponent.java`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| _m_alForeverAddList | ArrayList&lt;ForeverAddInfo&gt; | 永久加成列表 |
| _m_pcPlayerPropertyContainer | NPPlayerPropertyContainer | 玩家属性容器 |

### 2.2 永久加成信息 (ForeverAddInfo)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| itemId | long | 加成ID |
| itemCount | int | 加成次数 |

---

## 三、物品接口实现

### 3.1 物品类型

永久加成实现 `_IUserItemBasicDealer` 接口，作为物品处理：

```java
@Override
public ENPItemType getItemType()
{
    return ENPItemType.FOREVER_ADD;
}
```

### 3.2 物品操作

| 方法名 | 说明 |
|--------|------|
| getItemCount | 获取加成次数 |
| hasItem | 检查是否有足够加成 |
| initGainItem | 初始化时获得加成 |
| gainItem | 获得加成 |
| spendItem | 消耗加成 |

---

## 四、核心逻辑

### 4.1 获得永久加成

```java
public void gainForeverAdd(long _itemId, int _count, boolean _isNotMerge, NPPlayerContext _context)
{
    // 1. 查找是否已存在
    ForeverAddInfo info = lookup(_itemId);

    if (info == null)
    {
        // 2. 检查加成上限
        RefPlayerForeverAdd ref = RefPlayerForeverAdd.getMgr().get(_itemId);
        if (ref.add_limit > 0)
            _count = Math.min(_count, ref.add_limit);

        // 3. 创建新数据
        PlayerForeverAddBO bo = new PlayerForeverAddBO();
        bo.setCid(getBM(), getUserData().getCid());
        bo.setItemId(getBM(), _itemId);
        bo.setItemCount(getBM(), _count);
        bo.insert(getBM());

        // 4. 更新玩家属性
        getPlayerPropertyContainer().addModifier(ref.player_pro_add, _count);

        // 5. 推送数据
        getUserData().sendMsgToGC(US2GCWriter_021_PlayerInfo.make_075_OnForeverAddChg(info));
    }
    else
    {
        // 更新已存在的数据
        info.setCount((_count + info.getItemCount()), _context);
    }
}
```

### 4.2 消耗永久加成

```java
public boolean spendForeverAdd(long _itemId, int _count, NPPlayerContext _context)
{
    ForeverAddInfo info = lookup(_itemId);
    if (info == null || info.getItemCount() < _count)
        return false;

    info.setCount(_count, _context);
    return true;
}
```

---

## 五、数据库设计

### 5.1 永久加成数据表

**表名**: `player_forever_add`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键 |
| cid | BIGINT | 玩家CID |
| item_id | BIGINT | 加成ID |
| item_count | INT | 加成次数 |

---

## 六、协议构造

### 6.1 构造数据协议

```java
public void makeProto(ArrayList<NPCommon_ForeverAddInfo> _list)
{
    getUserData().lockUser();
    try
    {
        for (ForeverAddInfo info : _m_alForeverAddList)
        {
            _list.add(info.toProto());
        }
    }
    finally
    {
        getUserData().unlockUser();
    }
}
```

---

*文档生成时间: 2026-04-06*
