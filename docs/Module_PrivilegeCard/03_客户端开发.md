# 权益卡客户端开发文档

## 一、组件数据层

### 1.1 核心组件类

#### PrivilegeCardComponent
**路径**: `GameComponent/PrivilegeCard/PrivilegeCardComponent.cs`

**功能**: 管理玩家所有权益卡数据

**继承关系**:
```
PrivilegeCardComponent : _ANPBasicPlayerComponent
```

**主要属性**:

| 属性名 | 类型 | 说明 |
|--------|------|------|
| playerPropertyContainer | NPPlayerPropertyContainer | 玩家属性容器 |

---

## 二、数据管理

### 2.1 权益卡字典

组件使用字典管理权益卡：

```csharp
[NotNull] private Dictionary<EPrivilegeCardType, PrivilegeCardInfo> _m_dPrivilegeCardDic = new Dictionary<EPrivilegeCardType, PrivilegeCardInfo>();
```

### 2.2 属性加成管理

```csharp
[NotNull] private readonly CommonUnionBonusMgr _m_privilegeCardBonusMgr = new CommonUnionBonusMgr(EUnionBonusMgrTag.PRIVILEGE_CARD);
```

---

## 三、主要方法

### 3.1 获取权益卡信息

```csharp
// 获取权益卡信息
PrivilegeCardInfo cardInfo = NPPlayer.instance.privilegeCardComponent.getPrivilegeCardInfo(EPrivilegeCardType.MONTH);

// 检查是否激活
bool isActivate = cardInfo != null && cardInfo.isActivate;

// 检查是否可领取今日奖励
bool canGetReward = cardInfo.canGetRewardToday;
```

### 3.2 获取属性加成值

```csharp
// 获取属性加成值
long addValue = NPPlayer.instance.privilegeCardComponent.getAddPropValue(
    EPrivilegeCardType.MONTH,
    EBonusPropertyType.SPEED
);
```

---

## 四、请求方法

### 4.1 初始化请求

```csharp
// 请求权益卡初始化
NPPlayer.instance.privilegeCardComponent.reqPrivilegeCardInit();
```

### 4.2 领取每日奖励

```csharp
// 领取每日奖励
NPPlayer.instance.privilegeCardComponent.reqGainPrivilegeCardDailyReward(EPrivilegeCardType.MONTH, (succ, msg) =>
{
    if (succ)
    {
        Debug.Log("领取成功");
    }
});
```

---

## 五、消息事件

### 5.1 权益卡相关消息

| 消息类型 | 说明 |
|----------|------|
| ON_PRIVILEGE_CARD_CHG | 权益卡变化 |
| ON_PRIVILEGE_CARD_REMOVE | 权益卡移除 |
| ON_CROSS_DAY | 跨天事件 |

---

## 六、定时检查

### 6.1 过期检查

组件会定时检查权益卡是否过期：

```csharp
private void _tickCheck()
{
    // 检查过期权益卡
    foreach (PrivilegeCardInfo cardInfo in _m_dPrivilegeCardDic.Values)
    {
        if (cardInfo != null && !cardInfo.isActivate)
        {
            _m_lNeedRemoveType.Add(cardInfo.cardType);
        }
    }

    // 移除过期权益卡
    foreach (EPrivilegeCardType cardType in _m_lNeedRemoveType)
    {
        // 移除属性加成
        // 发送移除消息
    }
}
```

---

## 七、红点系统

### 7.1 红点刷新

```csharp
private void _refreshRedTip()
{
    long redTipCount = 0;
    foreach (PrivilegeCardInfo cardInfo in _m_dPrivilegeCardDic.Values)
    {
        if (cardInfo != null && cardInfo.canGetRewardToday)
            redTipCount++;
    }
    RedTipMgr.instance.setCountByRefRedTipId(RedTipConst.RED_PRIVILEGE_CARD, redTipCount);
}
```

---

## 八、UI窗口开发

### 8.1 权益卡界面

**窗口目录**: `GameWndGui/GGui/PrivilegeCard/`

| 类名 | 说明 |
|------|------|
| GGUIWndPrivilegeCardPage | 权益卡页面 |
| GGUIWndSubPrivilegeCard | 权益卡子页面 |
| GGUIWndPrivilegeCardDescContainer | 权益卡描述容器 |

---

*文档生成时间: 2026-04-06*
