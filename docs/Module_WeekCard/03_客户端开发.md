# 周卡客户端开发文档

## 一、组件数据层

### 1.1 核心组件类

#### GWeekCardComponent
**路径**: `GameComponent/WeekCard/GWeekCardComponent.cs`

**功能**: 管理玩家周卡数据

**继承关系**:
```
GWeekCardComponent : _ANPBasicPlayerComponent
```

**主要属性**:

| 属性名 | 类型 | 说明 |
|--------|------|------|
| expireTimeTagS | int | 过期时间戳 |
| hadUseFreeTrial | bool | 是否使用过免费试用 |
| npcType | EWeekCardNPCType | NPC类型 |
| npcId | long | NPC ID |
| onWeekCardInfoChg | Action | 周卡信息变化事件 |

---

## 二、周卡状态判断

### 2.1 获取周卡状态

```csharp
// 获取周卡状态
public EWeekCardStat getWeekCardStat()
{
    if (!_m_hadUseFreeTrial)
        return EWeekCardStat.NONE;
    if (hasWeekCard())
        return EWeekCardStat.IN_WEEK_CARD;
    return EWeekCardStat.EXPIREED;
}

// 判断周卡是否生效
public bool hasWeekCard()
{
    return NPPlayer.instance.weekCardComp.expireTimeTagS > FpsAndPingMgr.instance.serverTimeTagS;
}
```

---

## 三、设置信息管理

### 3.1 获取设置信息

```csharp
// 获取设置信息列表
List<_AGWeekCardSettingInfoBase> settingList = new List<_AGWeekCardSettingInfoBase>();
NPPlayer.instance.weekCardComp.getSettingInfoList(settingList);

// 获取指定类型设置
_AGWeekCardSettingInfoBase setting = NPPlayer.instance.weekCardComp.getSettingInfo(EWeekCardSettleType.LEVY);
```

### 3.2 设置信息类型

| 类型 | 类名 | 说明 |
|------|------|------|
| LEVY | GWeekCardSettingInfo_DealPolicy | 征收设置 |
| ANECDOTE | GWeekCardSettingInfo_DealPolicy | 政务设置 |
| CONSORT_RND_CALL | GWeekCardSettingInfo_DealPolicy | 妃子倾诉设置 |
| COLLEGE_STUDY | GWeekCardSettingInfo_CollegeStudy | 大学学习设置 |
| TRAVEL | GWeekCardSettingInfo_DealPolicy | 游历设置 |

---

## 四、请求方法

### 4.1 初始化请求

```csharp
// 请求周卡初始化
NPPlayer.instance.weekCardComp.presendInitProtocol();
```

### 4.2 请求结算信息

```csharp
// 请求结算信息
NPPlayer.instance.weekCardComp.reqWeekCardSettleInfo((msg) =>
{
    if (msg != null)
    {
        // 处理结算信息
        _processSettleInfo(msg.getSettleInfo());
    }
});
```

### 4.3 修改设置

```csharp
// 修改周卡设置
WeekCard_SingleSettingInfo settingInfo = new WeekCard_SingleSettingInfo();
settingInfo.setType(EWeekCardSettleType.LEVY);
// ... 设置数据

NPPlayer.instance.weekCardComp.reqWeekCardChgSetting(settingInfo, (msg) =>
{
    Debug.Log("设置修改成功");
});
```

### 4.4 激活免费试用

```csharp
// 激活免费试用
NPPlayer.instance.weekCardComp.reqWeekCardActiveFreeTrial((msg) =>
{
    if (msg != null)
    {
        Debug.Log("免费试用激活成功");
    }
});
```

### 4.5 修改NPC形象

```csharp
// 修改NPC形象
NPPlayer.instance.weekCardComp.reqWeekCardChgNPC(
    EWeekCardNPCType.CONSORT,
    consortId,
    (msg) =>
    {
        Debug.Log("NPC形象修改成功");
    }
);
```

---

## 五、执政官形象

### 5.1 获取执政官形象

```csharp
// 获取执政官形象索引
NPGGoIndex tdShowIndex = NPPlayer.instance.weekCardComp.getTDShowIndex();
```

---

## 六、消息事件

### 6.1 周卡相关消息

| 消息类型 | 说明 |
|----------|------|
| ON_WEEK_CARD_CHG | 周卡信息变化 |

---

## 七、UI窗口开发

### 7.1 周卡界面

**窗口目录**: `GameWndGui/GGui/WeekCard/`

| 类名 | 说明 |
|------|------|
| GGUIWndWeekCard | 周卡主界面 |
| GGUIWndWeekCardSettle | 结算界面 |
| GGUIWndWeekCardAssign | 分配界面 |
| GGUIWndWeekCardSelectedHero | 选择英雄界面 |

---

## 八、红点系统

### 8.1 红点刷新

```csharp
private void _refreshRed()
{
    // 检查是否有离线结算奖励
    if (hasSettleReward)
    {
        RedTipMgr.instance.setCountByRefRedTipId(RedTipConst.WEEK_CARD, 1);
    }
    else
    {
        RedTipMgr.instance.clearByRefRedTipId(RedTipConst.WEEK_CARD);
    }
}
```

---

*文档生成时间: 2026-04-06*
