# K2C 游戏框架 - 模块依赖关系图

## 一、模块总览

K2C 游戏框架共包含 **49 个功能模块**，按功能可分为以下几类：

| 分类 | 模块 | 说明 |
|------|------|------|
| **核心基础** | Player, Bag, Init | 玩家基础数据、背包、初始化 |
| **养成系统** | Hero, Consort, Child, Building | 英雄、配偶、子嗣、建筑养成 |
| **社交系统** | Guild, Chat, Mail, Friend | 公会、聊天、邮件、好友 |
| **任务系统** | Quest, Achieve, StageGoal | 主线任务、成就、阶段目标 |
| **经济系统** | Shop, Recruit, Bonus | 商店、招募、奖励 |
| **活动系统** | Activity, SimpleActivity, SevenDayLogin, WeekCard | 各类活动模块 |
| **战斗系统** | Arena, Dungeon, GuildDungeon | 竞技场、副本、公会副本 |
| **资源系统** | NPResource, PlayerBuff, Rank | 资源管理、Buff、排行榜 |
| **特殊玩法** | Mars, MarsBuilding, MarsExplore, MarsPeople | 火星玩法系列 |
| **扩展功能** | Travel, Dinner, Inn, Museum, TreasureHunt | 旅行、宴席、客栈、博物馆、寻宝 |

---

## 二、模块依赖关系图

### 2.1 核心模块依赖关系

```mermaid
graph TB
    subgraph 核心层["核心层（被所有模块依赖）"]
        Player["Player<br/>玩家模块"]
        Bag["Bag<br/>背包模块"]
        Init["Init<br/>初始化模块"]
        NPResource["NPResource<br/>资源模块"]
    end

    subgraph 数据层["数据层（配置与缓存）"]
        ClientData["ClientData<br/>客户端数据"]
        FuncUnlock["FuncUnlock<br/>功能解锁"]
        RedDot["RedDot<br/>红点系统"]
    end

    subgraph 养成层["养成系统"]
        Hero["Hero<br/>英雄模块"]
        Consort["Consort<br/>配偶模块"]
        Child["Child<br/>子嗣模块"]
        Building["Building<br/>建筑模块"]
        PlayerSkin["PlayerSkin<br/>皮肤模块"]
        PlayerTitle["PlayerTitle<br/>称号模块"]
    end

    subgraph 社交层["社交系统"]
        Guild["Guild<br/>公会模块"]
        Chat["Chat<br/>聊天模块"]
        Mail["Mail<br/>邮件模块"]
    end

    subgraph 任务层["任务系统"]
        Quest["Quest<br/>任务模块"]
        Achieve["Achieve<br/>成就模块"]
        StageGoal["StageGoal<br/>阶段目标"]
    end

    %% 核心依赖
    Bag --> Player
    Init --> Player
    NPResource --> Player

    %% 养成系统依赖
    Hero --> Player
    Hero --> Bag
    Hero --> NPResource
    Consort --> Player
    Consort --> Hero
    Child --> Player
    Child --> Consort
    Child --> Hero
    Building --> Player
    Building --> Hero
    PlayerSkin --> Player
    PlayerTitle --> Player

    %% 社交系统依赖
    Guild --> Player
    Guild --> Chat
    Guild --> Mail
    Chat --> Player
    Mail --> Player

    %% 任务系统依赖
    Quest --> Player
    Quest --> Bag
    Quest --> Hero
    Quest --> Building
    Quest --> Guild
    Achieve --> Player
    Achieve --> Quest
    StageGoal --> Player
    StageGoal --> Quest

    %% 数据层依赖
    ClientData --> Player
    FuncUnlock --> Player
    RedDot --> Player
    RedDot --> Quest
    RedDot --> Mail
```

### 2.2 养成系统依赖关系

```mermaid
graph LR
    subgraph 英雄养成
        Hero["Hero<br/>英雄模块"]
        Equip["Equip<br/>装备（Hero子模块）"]
        HeroSkin["HeroSkin<br/>英雄皮肤"]
        Halo["Halo<br/>光环系统"]
    end

    subgraph 家族养成
        Consort["Consort<br/>配偶模块"]
        Child["Child<br/>子嗣模块"]
    end

    subgraph 建筑养成
        Building["Building<br/>建筑模块"]
        Inn["Inn<br/>客栈模块"]
    end

    subgraph 外观养成
        PlayerSkin["PlayerSkin<br/>玩家皮肤"]
        PlayerTitle["PlayerTitle<br/>称号模块"]
        Icon["Icon<br/>头像模块"]
    end

    subgraph 核心依赖
        Player["Player"]
        Bag["Bag"]
        Buff["PlayerBuff"]
    end

    %% 英雄依赖
    Equip --> Hero
    HeroSkin --> Hero
    Halo --> Hero
    Hero --> Player
    Hero --> Bag
    Hero --> Buff

    %% 家族依赖
    Consort --> Player
    Consort --> Hero
    Child --> Consort
    Child --> Player

    %% 建筑依赖
    Building --> Player
    Building --> Hero
    Inn --> Building
    Inn --> Hero

    %% 外观依赖
    PlayerSkin --> Player
    PlayerTitle --> Player
    Icon --> Player
```

### 2.3 活动系统依赖关系

```mermaid
graph TB
    subgraph 核心活动
        Activity["Activity<br/>活动模块"]
        SimpleActivity["SimpleActivity<br/>简单活动"]
    end

    subgraph 登录活动
        SevenDayLogin["SevenDayLogin<br/>七日登录"]
        WeekCard["WeekCard<br/>周卡"]
        PrivilegeCard["PrivilegeCard<br/>特权卡"]
    end

    subgraph 组队活动
        ActivityTeam["ActivityTeam<br/>组队活动"]
        FristTeamActivity["FristTeamActivity<br/>首队活动"]
    end

    subgraph 特殊活动
        TreasureHunt["TreasureHunt<br/>寻宝活动"]
        Bonus["Bonus<br/>奖励模块"]
        StageGoal["StageGoal<br/>阶段目标"]
    end

    subgraph 核心依赖
        Player["Player"]
        Bag["Bag"]
        Quest["Quest"]
    end

    %% 活动依赖
    Activity --> Player
    Activity --> Bag
    Activity --> Quest
    SimpleActivity --> Activity

    %% 登录活动依赖
    SevenDayLogin --> Activity
    SevenDayLogin --> Player
    WeekCard --> Player
    WeekCard --> Bag
    PrivilegeCard --> Player

    %% 组队活动依赖
    ActivityTeam --> Activity
    ActivityTeam --> Guild
    FristTeamActivity --> Activity

    %% 特殊活动依赖
    TreasureHunt --> Activity
    TreasureHunt --> Bag
    Bonus --> Player
    Bonus --> Bag
    StageGoal --> Player
    StageGoal --> Quest

    %% 隐式依赖
    Guild["Guild"]
```

### 2.4 社交系统依赖关系

```mermaid
graph TB
    subgraph 公会系统
        Guild["Guild<br/>公会模块"]
        GuildDungeon["GuildDungeon<br/>公会副本"]
        GuildRelated["GuildRelated<br/>公会相关"]
    end

    subgraph 通信系统
        Chat["Chat<br/>聊天模块"]
        Mail["Mail<br/>邮件模块"]
    end

    subgraph 玩家展示
        Rank["Rank<br/>排行榜"]
        PlayerShow["PlayerShow<br/>玩家展示"]
    end

    subgraph 核心依赖
        Player["Player"]
        Hero["Hero"]
        NPResource["NPResource"]
    end

    %% 公会依赖
    Guild --> Player
    Guild --> Chat
    Guild --> Mail
    Guild --> Hero
    GuildDungeon --> Guild
    GuildDungeon --> Hero
    GuildRelated --> Guild

    %% 通信依赖
    Chat --> Player
    Chat --> Guild
    Mail --> Player
    Mail --> Bag

    %% 展示依赖
    Rank --> Player
    Rank --> Hero
    Rank --> Guild
    PlayerShow --> Player
    PlayerShow --> Hero
    PlayerShow --> Guild

    %% 交叉依赖
    NPResource --> Player
```

### 2.5 火星玩法系统依赖关系

```mermaid
graph TB
    subgraph 火星核心
        Mars["Mars<br/>火星主模块"]
    end

    subgraph 火星建筑
        MarsBuilding["MarsBuilding<br/>火星建筑"]
    end

    subgraph 火星探索
        MarsExplore["MarsExplore<br/>火星探索"]
    end

    subgraph 火星人物
        MarsPeople["MarsPeople<br/>火星人物"]
    end

    subgraph 核心依赖
        Player["Player"]
        Hero["Hero"]
        Building["Building"]
        Guild["Guild"]
    end

    %% 火星模块依赖
    Mars --> Player
    Mars --> Hero
    Mars --> Building

    MarsBuilding --> Mars
    MarsBuilding --> Player
    MarsBuilding --> NPResource

    MarsExplore --> Mars
    MarsExplore --> Hero
    MarsExplore --> Guild

    MarsPeople --> Mars
    MarsPeople --> Player

    NPResource["NPResource"]
```

---

## 三、模块依赖矩阵

### 3.1 核心依赖统计

| 模块 | 被依赖数 | 依赖模块数 | 核心程度 |
|------|----------|------------|----------|
| Player | 45+ | 0 | ⭐⭐⭐⭐⭐ 核心 |
| Bag | 30+ | 1 | ⭐⭐⭐⭐⭐ 核心 |
| Hero | 20+ | 3 | ⭐⭐⭐⭐ 重要 |
| Quest | 15+ | 5 | ⭐⭐⭐⭐ 重要 |
| Guild | 12+ | 4 | ⭐⭐⭐ 中等 |
| NPResource | 15+ | 1 | ⭐⭐⭐⭐ 重要 |
| Mail | 8+ | 2 | ⭐⭐⭐ 中等 |
| Chat | 6+ | 2 | ⭐⭐⭐ 中等 |
| Building | 8+ | 2 | ⭐⭐⭐ 中等 |
| Activity | 10+ | 3 | ⭐⭐⭐ 中等 |

### 3.2 依赖方向说明

```mermaid
flowchart LR
    subgraph 上游模块["上游模块（被依赖）"]
        A[Player]
        B[Bag]
        C[NPResource]
    end

    subgraph 中游模块["中游模块"]
        D[Hero]
        E[Quest]
        F[Building]
    end

    subgraph 下游模块["下游模块（依赖者）"]
        G[Guild]
        H[Arena]
        I[Activity]
    end

    A --> D
    A --> E
    A --> F
    B --> D
    B --> E
    C --> D

    D --> G
    D --> H
    E --> I
    F --> G
```

---

## 四、模块分层架构

### 4.1 四层架构图

```mermaid
graph TB
    subgraph L1["第一层：基础设施层"]
        L1_1[Player]
        L1_2[Bag]
        L1_3[NPResource]
        L1_4[Init]
        L1_5[ClientData]
    end

    subgraph L2["第二层：核心玩法层"]
        L2_1[Hero]
        L2_2[Building]
        L2_3[Quest]
        L2_4[Achieve]
        L2_5[FuncUnlock]
    end

    subgraph L3["第三层：扩展玩法层"]
        L3_1[Guild]
        L3_2[Arena]
        L3_3[Dungeon]
        L3_4[Shop]
        L3_5[Activity]
        L3_6[Consort]
        L3_7[Child]
    end

    subgraph L4["第四层：高级功能层"]
        L4_1[GuildDungeon]
        L4_2[Mars]
        L4_3[TreasureHunt]
        L4_4[Inn]
        L4_5[Dinner]
    end

    L1 --> L2
    L2 --> L3
    L3 --> L4
```

### 4.2 层级职责说明

| 层级 | 职责 | 模块特点 |
|------|------|----------|
| **基础设施层** | 提供基础数据存储和管理 | 被所有上层模块依赖，修改影响面大 |
| **核心玩法层** | 实现游戏核心养成循环 | 依赖基础层，被扩展层依赖 |
| **扩展玩法层** | 提供丰富的游戏内容 | 依赖核心层，功能相对独立 |
| **高级功能层** | 复杂系统组合和特殊玩法 | 依赖多层，功能高度集成 |

---

## 五、循环依赖检测

### 5.1 检测结果

经分析，以下模块存在潜在循环依赖：

```mermaid
graph LR
    A[Quest] -->|任务目标| B[Hero]
    B -->|升级触发| A

    C[Guild] -->|公会任务| D[Quest]
    D -->|公会解锁| C

    E[Building] -->|产出资源| F[Player]
    F -->|解锁建筑| E
```

### 5.2 解决方案

上述循环依赖通过以下方式避免实际循环：

1. **事件驱动**：使用事件系统解耦，模块间通过事件通信而非直接调用
2. **接口隔离**：定义清晰的接口边界，避免交叉调用
3. **延迟处理**：使用异步处理机制，打破调用链

---

## 六、模块独立性评估

### 6.1 高独立性模块

| 模块 | 独立性 | 说明 |
|------|--------|------|
| Travel | ★★★★★ | 旅行模块，仅依赖 Player |
| Dinner | ★★★★★ | 宴席模块，仅依赖 Player、Hero |
| Museum | ★★★★☆ | 博物馆模块，依赖较少 |
| TreasureHunt | ★★★★☆ | 寻宝模块，依赖较少 |
| Rank | ★★★★☆ | 排行榜模块，只读依赖 |

### 6.2 高耦合模块

| 模块 | 耦合度 | 主要耦合对象 |
|------|--------|--------------|
| Guild | ★★★★★ | Chat, Mail, Hero, Player, Quest |
| Activity | ★★★★☆ | Player, Bag, Quest, Guild |
| Mars | ★★★★☆ | Player, Hero, Building, Guild |
| Quest | ★★★★☆ | Player, Bag, Hero, Building, Guild |

---

*文档生成时间: 2026-04-11*
