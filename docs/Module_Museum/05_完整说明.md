# 博物馆模块完整说明

## 一、模块概述

### 1.1 业务背景

博物馆模块是 K2C 游戏的收藏养成玩法，负责管理玩家的藏品收集和展示系统。玩家可以通过各种途径获取藏品，对藏品进行升级强化，激活藏品获得属性加成，构建属于自己的博物馆展示厅。

### 1.2 目标用户

- **收藏型玩家**：喜欢收集各类物品的玩家
- **养成型玩家**：追求长期养成的玩家
- **属性追求玩家**：追求角色属性提升的玩家
- **展示型玩家**：喜欢展示成就的玩家

### 1.3 核心价值

1. **收藏乐趣**：收集各类稀有藏品带来的成就感
2. **属性加成**：激活藏品获得永久属性提升
3. **展示系统**：展示藏品彰显玩家实力
4. **长期目标**：为玩家提供长期追求目标

---

## 二、功能清单

### 2.1 藏品收集功能

| 功能名称 | 功能描述 | 用户价值 |
|---------|---------|---------|
| 藏品获取 | 通过各种途径获取藏品 | 扩充藏品收集 |
| 藏品查看 | 查看已收集的藏品 | 了解藏品信息 |
| 藏品分类 | 按类型分类管理藏品 | 方便管理查找 |

### 2.2 藏品升级功能

| 功能名称 | 功能描述 | 用户价值 |
|---------|---------|---------|
| 藏品升级 | 消耗材料提升藏品等级 | 增强藏品属性 |
| 升级预览 | 预览升级后的属性 | 了解升级收益 |
| 批量升级 | 快速升级多个等级 | 节省操作时间 |

### 2.3 藏品激活功能

| 功能名称 | 功能描述 | 用户价值 |
|---------|---------|---------|
| 藏品激活 | 激活藏品获得属性加成 | 提升角色实力 |
| 激活状态 | 查看藏品激活状态 | 了解属性加成 |
| 激活限制 | 部分藏品需要满足条件 | 增加游戏目标 |

### 2.4 展示系统功能

| 功能名称 | 功能描述 | 用户价值 |
|---------|---------|---------|
| 展位管理 | 管理博物馆展位 | 规划藏品展示 |
| 藏品展示 | 在展位展示藏品 | 展示给其他玩家 |
| 展示奖励 | 展示获得额外收益 | 增加展示动力 |

---

## 三、业务流程详解

### 3.1 藏品获取流程

```mermaid
flowchart TD
    A[触发藏品获取] --> B{获取来源}
    B -->|活动奖励| C[发放活动藏品]
    B -->|副本掉落| D[发放副本藏品]
    B -->|商城购买| E[发放商城藏品]
    B -->|合成制作| F[发放合成藏品]
    C --> G[检查藏品是否已拥有]
    D --> G
    E --> G
    F --> G
    G -->|已拥有| H[转换为碎片/升阶材料]
    G -->|未拥有| I[新增藏品记录]
    H --> J[推送藏品变更]
    I --> J
```

**详细步骤说明**：

1. **获取来源**
   - 活动奖励：参与活动获取限定藏品
   - 副本掉落：挑战副本随机掉落
   - 商城购买：花费货币购买
   - 合成制作：收集材料合成

2. **去重处理**
   - 已有藏品转为碎片或材料
   - 新藏品直接添加到收藏

3. **状态更新**
   - 推送藏品变更通知
   - 更新收集进度

### 3.2 藏品升级流程

```mermaid
flowchart TD
    A[玩家请求升级藏品] --> B{检查藏品状态}
    B -->|藏品不存在| C[返回藏品不存在]
    B -->|藏品已满级| D[返回已满级]
    B -->|可升级| E{检查升级材料}
    E -->|材料不足| F[返回材料不足]
    E -->|材料充足| G[扣除升级材料]
    G --> H[藏品等级+1]
    H --> I[更新藏品属性]
    I --> J{检查是否激活}
    J -->|已激活| K[更新玩家属性]
    J -->|未激活| L[保持原状]
    K --> M[推送藏品变更]
    L --> M
```

**详细步骤说明**：

1. **状态检查**
   - 验证藏品是否存在
   - 验证是否达到等级上限

2. **材料消耗**
   - 检查材料数量是否充足
   - 扣除升级所需材料

3. **升级效果**
   - 藏品等级提升
   - 属性值增加

### 3.3 藏品激活流程

```mermaid
flowchart TD
    A[玩家请求激活藏品] --> B{检查藏品状态}
    B -->|藏品不存在| C[返回藏品不存在]
    B -->|藏品已激活| D[返回已激活]
    B -->|未激活| E{检查激活条件}
    E -->|条件不满足| F[返回条件不满足]
    E -->|条件满足| G[更新藏品激活状态]
    G --> H[计算属性加成]
    H --> I[更新玩家属性]
    I --> J[推送藏品变更]
    J --> K[推送玩家属性变更]
```

**详细步骤说明**：

1. **状态检查**
   - 验证藏品是否存在
   - 验证是否已激活

2. **条件验证**
   - 检查等级是否满足
   - 检查是否有足够激活材料

3. **激活效果**
   - 藏品激活状态变更
   - 属性加成生效

### 3.4 展示管理流程

```mermaid
flowchart TD
    A[玩家请求展示藏品] --> B{检查展位状态}
    B -->|展位已满| C[返回展位已满]
    B -->|展位空闲| D{检查藏品状态}
    D -->|藏品未解锁| E[返回藏品未解锁]
    D -->|藏品已解锁| F[将藏品放入展位]
    F --> G[更新展位信息]
    G --> H[计算展示收益]
    H --> I[推送展位变更]
```

**详细步骤说明**：

1. **展位检查**
   - 验证展位是否可用
   - 验证展位数量限制

2. **藏品验证**
   - 验证藏品是否已解锁
   - 验证藏品是否可用

3. **展示效果**
   - 更新展位显示
   - 计算展示收益

---

## 四、关键规则

### 4.1 藏品分类规则

1. **稀有度分类**
   - 普通：白色品质，基础属性
   - 稀有：蓝色品质，较高属性
   - 史诗：紫色品质，高额属性
   - 传说：橙色品质，顶级属性

2. **类型分类**
   - 武器类：提升攻击属性
   - 防具类：提升防御属性
   - 饰品类：提升特殊属性
   - 艺术品类：提升综合属性

### 4.2 藏品升级规则

1. **等级限制**
   - 不同稀有度有不同等级上限
   - 普通：最高 10 级
   - 稀有：最高 20 级
   - 史诗：最高 30 级
   - 传说：最高 50 级

2. **材料消耗**
   - 升级需要消耗对应材料
   - 等级越高消耗越多

3. **属性成长**
   - 每级属性按配置增长
   - 高稀有度成长更高

### 4.3 藏品激活规则

1. **激活条件**
   - 达到指定等级
   - 消耗激活材料
   - 完成特定任务

2. **激活效果**
   - 获得永久属性加成
   - 解锁藏品特殊效果

3. **激活限制**
   - 同类型藏品激活上限
   - 部分藏品需要前置激活

### 4.4 展示规则

1. **展位限制**
   - 展位数量有上限
   - 可通过升级博物馆扩展

2. **展示收益**
   - 展示藏品获得额外收益
   - 稀有度越高收益越多

---

## 五、用户故事

### 5.1 收藏玩家故事

> 作为一名热爱收藏的玩家，我每天都会查看博物馆的收集进度。今天我通过副本掉落获得了一件稀有藏品——"古代长剑"，这是我收藏品列表中的第 50 件藏品。看着博物馆中越来越多的藏品，我感到非常有成就感。接下来我的目标是收集全部传说级藏品。

### 5.2 属性追求玩家故事

> 作为一名追求实力的玩家，我非常重视藏品带来的属性加成。我优先升级和激活那些提供攻击力加成的藏品。经过一段时间的培养，我的攻击力提升了 15%，在战斗中明显感觉更强了。博物馆系统让我在不增加装备的情况下也能提升实力。

### 5.3 展示玩家故事

> 作为一名喜欢展示的玩家，我会把最稀有、最珍贵的藏品放在展位上展示。其他玩家参观我的博物馆时都能看到这些藏品，这让我感到非常自豪。有时候还有玩家专门来询问某件藏品的获取途径，我会热心地告诉他们。

---

## 六、示例

### 6.1 藏品获取示例

**输入数据**：
- 获取来源：活动奖励
- 藏品ID：10001
- 藏品名称：古代长剑
- 稀有度：稀有（蓝色）

**获取结果**：
```
藏品ID：10001
藏品名称：古代长剑
稀有度：稀有
等级：1
状态：未激活
属性加成：攻击+10
```

### 6.2 藏品升级示例

**初始状态**：
```
藏品：古代长剑（ID: 10001）
当前等级：5
当前属性：攻击+50
```

**升级操作**：
```
目标等级：6
消耗材料：藏品碎片 × 10、金币 × 500
```

**升级结果**：
```
等级：6
属性：攻击+60（+10）
材料已扣除
```

### 6.3 藏品激活示例

**激活条件**：
```
藏品：古代长剑（ID: 10001）
等级要求：5级
材料消耗：激活石 × 1
```

**激活结果**：
```
激活状态：已激活
属性加成：攻击+50（永久生效）
特殊效果：暴击率+2%
```

### 6.4 展示管理示例

**展示操作**：
```
展位：#3
藏品：传说王冠（ID: 20001）
稀有度：传说
```

**展示结果**：
```
展位状态：已展示
展示收益：金币 × 100/小时
访客可见：是
```

---

---

## 七、系统架构图

```mermaid
graph TB
    subgraph 客户端
        A[MuseumComponent<br/>博物馆组件]
        B[MuseumUIPanel<br/>博物馆界面]
        C[MuseumItemManager<br/>藏品管理器]
        D[MuseumSlotManager<br/>展位管理器]
    end

    subgraph 服务端
        E[MuseumService<br/>博物馆服务]
        F[MuseumItemService<br/>藏品服务]
        G[MuseumSlotService<br/>展位服务]
        H[MuseumSetService<br/>套装服务]
    end

    subgraph 数据层
        I[(MySQL<br/>玩家数据)]
        J[(Redis<br/>缓存)]
        K[Config<br/>配置数据]
    end

    B --> A
    A --> C
    A --> D

    A --> E
    E --> F
    E --> G
    E --> H

    F --> I
    F --> J
    F --> K
    G --> I
    H --> I
```

---

## 八、数据流程图

### 8.1 藏品获取数据流

```mermaid
flowchart LR
    A[获取来源] --> B{检查是否已拥有}
    B -->|已拥有| C[转换为碎片]
    B -->|未拥有| D[创建藏品记录]

    C --> E[更新碎片数量]
    D --> F[初始化等级=1]

    E --> G[推送OnItemChg]
    F --> H[推送OnItemAdd]

    H --> I[检查套装]
    I --> J[更新套装进度]
    J --> K[推送OnSetEffect]
```

### 8.2 属性计算数据流

```mermaid
flowchart TD
    A[属性计算请求] --> B[获取已激活藏品列表]
    B --> C[遍历每个藏品]

    C --> D[计算基础属性]
    D --> E[加上等级成长]
    E --> F[加上激活效果]
    F --> G[加上多阶段效果]

    G --> H{是否还有藏品}
    H -->|是| C
    H -->|否| I[汇总所有属性]

    I --> J[加上套装效果]
    J --> K[返回总属性]
```

---

## 九、配置示例汇总

### 9.1 完整藏品配置示例

```json
{
    "item_id": 10001,
    "item_name": "古代长剑",
    "item_desc": "一把古老的长剑，剑身已布满岁月的痕迹",
    "item_type": 1,
    "rarity": 2,
    "max_level": 20,
    "base_attr": {
        "atk": 10,
        "crit_rate": 0.01
    },
    "growth_rate": {
        "atk": 2,
        "crit_rate": 0.002
    },
    "activate_condition": {
        "level": 5
    },
    "activate_cost": {
        "item:stone_activate": 1
    },
    "activate_effect": {
        "crit_dmg": 0.1
    },
    "icon": "ui/museum/item_sword_01",
    "model": "models/museum/sword_01",
    "sort_weight": 100,
    "fragment_id": 100011,
    "decompose_reward": {
        "item:frag_museum": 3
    }
}
```

### 9.2 完整套装配置示例

```json
{
    "set_id": 1,
    "set_name": "远古战士套装",
    "set_desc": "远古战士遗留的装备，蕴含着强大的力量",
    "item_ids": [10001, 10002, 20002, 30001, 40001, 40002],
    "effects": {
        "2": {
            "attr:atk": 50
        },
        "4": {
            "attr:atk": 100,
            "attr:crit_rate": 0.05
        },
        "6": {
            "attr:all_attr": 0.05
        }
    }
}
```

---

## 十、常见问题与解决方案

### 10.1 问题排查流程图

```mermaid
flowchart TD
    A[用户反馈问题] --> B{问题类型}

    B -->|藏品无法升级| C[检查等级上限]
    C --> D[检查材料数量]
    D --> E[检查成功率]

    B -->|藏品无法激活| F[检查激活条件]
    F --> G[检查等级要求]
    G --> H[检查前置激活]

    B -->|属性未生效| I[检查激活状态]
    I --> J[检查缓存状态]
    J --> K[清理缓存重试]

    B -->|展位异常| L[检查展位状态]
    L --> M[检查藏品状态]
    M --> N[检查锁定状态]
```

### 10.2 错误处理速查表

| 错误现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 升级按钮灰显 | 满级或材料不足 | 检查等级和材料 |
| 激活无响应 | 条件不满足 | 查看激活条件提示 |
| 属性面板不更新 | 缓存未刷新 | 刷新界面或重登 |
| 展位无法放入 | 已在其他展位 | 先移出原展位 |
| 套装效果不显示 | 激活件数不足 | 收集更多套装件 |

---

## 十一、详细业务流程图

### 11.1 藏品获取完整业务流程

```mermaid
flowchart TD
    A[触发藏品获取事件] --> B{获取来源判断}

    B -->|副本掉落| C1[获取副本配置]
    B -->|活动奖励| C2[获取活动配置]
    B -->|商城购买| C3[获取商城配置]
    B -->|合成制作| C4[获取合成配置]
    B -->|任务奖励| C5[获取任务配置]

    C1 --> D[计算掉落数量]
    C2 --> D
    C3 --> D
    C4 --> D
    C5 --> D

    D --> E[获取藏品配置]
    E --> F{检查玩家是否已拥有}

    F -->|已拥有| G1[转换为碎片]
    F -->|未拥有| G2[创建藏品记录]

    G1 --> H1[更新碎片数量]
    H1 --> I1[推送OnMuseumItemChg]

    G2 --> H2[初始化藏品数据]
    H2 --> H3[检查套装归属]
    H3 --> H4[更新收集进度]
    H4 --> I2[推送OnMuseumItemAdd]

    I1 --> J[记录获取日志]
    I2 --> J

    J --> K[触发成就检测]
    K --> L[触发任务进度]
    L --> M[结束]
```

### 11.2 藏品升级完整业务流程

```mermaid
flowchart TD
    A[升级请求] --> B[验证请求参数]
    B --> C{参数是否合法}

    C -->|否| D1[返回参数错误]
    C -->|是| E[获取藏品数据]

    E --> F{藏品是否存在}
    F -->|否| D2[返回藏品不存在]
    F -->|是| G[获取藏品配置]

    G --> H{是否已满级}
    H -->|是| D3[返回已满级]
    H -->|否| I[获取升级配置]

    I --> J[计算升级消耗]
    J --> K{材料是否充足}

    K -->|否| D4[返回材料不足]
    K -->|是| L{是否使用保护石}

    L -->|是| M1[检查保护石数量]
    M1 --> M2{保护石充足}
    M2 -->|否| D5[返回保护石不足]
    M2 -->|是| N

    L -->|否| N[扣除升级材料]
    N --> O[执行成功率判定]

    O --> P{判定结果}

    P -->|成功| Q1[等级+1]
    Q1 --> Q2[更新属性]
    Q2 --> Q3[重置失败计数]
    Q3 --> R

    P -->|失败并使用保护| R1[等级不变]
    R1 --> R2[扣除保护石]
    R2 --> R

    P -->|失败未使用保护| S1[等级-1]
    S1 --> S2[增加失败计数]
    S2 --> R

    R --> T{是否继续升级}
    T -->|是且满足条件| I
    T -->|否| U[保存数据库]

    U --> V{藏品是否激活}
    V -->|是| W1[更新玩家属性]
    W1 --> W2[推送属性变更]
    V -->|否| X

    W2 --> X[推送藏品变更]
    X --> Y[记录升级日志]
    Y --> Z[结束]
```

### 11.3 藏品激活完整业务流程

```mermaid
flowchart TD
    A[激活请求] --> B[验证请求参数]
    B --> C{参数是否合法}

    C -->|否| D1[返回参数错误]
    C -->|是| E[获取藏品数据]

    E --> F{藏品是否存在}
    F -->|否| D2[返回藏品不存在]
    F -->|是| G{是否已激活该阶段}

    G -->|是| D3[返回已激活]
    G -->|否| H[获取激活配置]

    H --> I[检查激活条件]

    I --> J{等级是否满足}
    J -->|否| D4[返回等级不足]
    J -->|是| K{任务是否完成}

    K -->|否| D5[返回任务未完成]
    K -->|是| L{前置藏品是否激活}

    L -->|否| D6[返回前置未激活]
    L -->|是| M[检查激活材料]

    M --> N{材料是否充足}
    N -->|否| D7[返回材料不足]
    N -->|是| O[扣除激活材料]

    O --> P[更新激活状态]
    P --> Q[更新激活阶段]

    Q --> R[计算属性变化]
    R --> S[更新套装激活数]
    S --> T[计算套装效果]

    T --> U[更新玩家总属性]
    U --> V[使属性缓存失效]

    V --> W[推送藏品变更]
    W --> X[推送属性变更]

    X --> Y{是否触发套装激活}
    Y -->|是| Z1[推送套装效果]
    Y -->|否| Z2

    Z1 --> Z2[记录激活日志]
    Z2 --> AA[触发成就检测]
    AA --> AB[结束]
```

### 11.4 展位管理完整业务流程

```mermaid
flowchart TD
    A[展位操作请求] --> B{操作类型}

    B -->|设置展位| C1[获取展位数据]
    B -->|清空展位| C2[获取展位数据]

    C1 --> D{展位是否锁定}
    C2 --> D

    D -->|是| E1[返回展位锁定]
    D -->|否| F{操作类型}

    F -->|设置| G1{展位是否已有藏品}
    G1 -->|是| H1[移出原藏品]
    G1 -->|否| I1

    H1 --> I1[获取新藏品数据]
    I1 --> J1{新藏品是否在其他展位}
    J1 -->|是| K1[清空原展位]
    J1 -->|否| L1
    K1 --> L1[放入新展位]

    L1 --> M1[更新展位数据]
    M1 --> M2[计算展示收益]
    M2 --> M3[更新藏品展位索引]

    F -->|清空| G2{展位是否有藏品}
    G2 -->|否| H2[直接返回成功]
    G2 -->|是| I2[获取展位藏品]

    I2 --> J2[清除藏品展位索引]
    J2 --> K2[清空展位数据]

    M3 --> N[保存数据库]
    K2 --> N

    N --> O[推送展位变更]
    O --> P[计算总展示收益]
    P --> Q[推送收益变更]
    Q --> R[记录操作日志]
    R --> S[结束]
```

### 11.5 合成系统完整业务流程

```mermaid
flowchart TD
    A[合成请求] --> B[验证请求参数]
    B --> C{参数是否合法}

    C -->|否| D1[返回参数错误]
    C -->|是| E[获取碎片配置]

    E --> F{碎片配置是否存在}
    F -->|否| D2[返回配置不存在]
    F -->|是| G[获取关联藏品配置]

    G --> H[计算所需碎片数量]
    H --> I{碎片是否充足}

    I -->|否| D3[返回碎片不足]
    I -->|是| J{是否达到合成上限}

    J -->|是| D4[返回已达上限]
    J -->|否| K[扣除碎片]

    K --> L{是否已拥有藏品}
    L -->|是| M1[转换为碎片]
    M1 --> N1[更新碎片数量]
    N1 --> O1[推送OnMuseumItemChg]

    L -->|否| M2[创建藏品记录]
    M2 --> N2[初始化藏品数据]
    N2 --> O2[推送OnMuseumItemAdd]

    O1 --> P[更新今日合成次数]
    O2 --> P

    P --> Q[记录合成日志]
    Q --> R[触发任务进度]
    R --> S[结束]
```

### 11.6 分解系统完整业务流程

```mermaid
flowchart TD
    A[分解请求] --> B[验证请求参数]
    B --> C{参数是否合法}

    C -->|否| D1[返回参数错误]
    C -->|是| E[获取藏品数据]

    E --> F{藏品是否存在}
    F -->|否| D2[返回藏品不存在]
    F -->|是| G{藏品是否可分解}

    G -->|否| D3[返回不可分解]
    G -->|是| H{藏品是否在展示}

    H -->|是| D4[返回正在展示]
    H -->|否| I{藏品是否已激活}

    I -->|是| D5[返回已激活不可分解]
    I -->|否| J[获取分解奖励配置]

    J --> K[删除藏品数据]
    K --> L[发放分解奖励]
    L --> M[推送OnMuseumItemDel]
    M --> N[推送背包更新]
    N --> O[记录分解日志]
    O --> P[结束]
```

---

## 十二、系统状态转换图

### 12.1 藏品状态完整转换

```mermaid
stateDiagram-v2
    [*] --> 未解锁: 藏品数据创建
    未解锁 --> 已解锁: 获得藏品碎片合成
    未解锁 --> 已解锁: 直接获取藏品

    已解锁 --> 升级中: 开始升级
    升级中 --> 已解锁: 升级完成(未满级)
    升级中 --> 已满级: 升级完成(达到上限)

    已解锁 --> 待激活: 满足激活条件
    待激活 --> 已激活: 执行激活操作
    待激活 --> 已解锁: 激活取消

    已激活 --> 二阶段激活: 完成二阶段激活条件
    二阶段激活 --> 已激活: 二阶段激活成功

    已激活 --> 展示中: 放入展位
    二阶段激活 --> 展示中: 放入展位
    展示中 --> 已激活: 移出展位
    展示中 --> 二阶段激活: 移出展位(二阶段)

    已解锁 --> 已分解: 执行分解
    已激活 --> 己分解: 特殊分解(需确认)

    已满级 --> [*]: 达到最终状态
    已分解 --> [*]: 藏品销毁
```

### 12.2 展位状态完整转换

```mermaid
stateDiagram-v2
    [*] --> 锁定: 展位创建(需解锁)
    [*] --> 空闲: 展位创建(默认解锁)

    锁定 --> 空闲: 满足解锁条件
    空闲 --> 锁定: 特殊活动锁定

    空闲 --> 展示中: 放入藏品
    展示中 --> 空闲: 移出藏品

    展示中 --> 活动锁定: 活动期间锁定
    活动锁定 --> 展示中: 活动结束解锁

    锁定 --> [*]: 展位删除
    空闲 --> [*]: 展位删除
    展示中 --> [*]: 展位删除
```

### 12.3 套装效果激活状态

```mermaid
stateDiagram-v2
    [*] --> 未收集: 套装数据创建
    未收集 --> 单件激活: 激活1件藏品

    单件激活 --> 二件效果: 激活2件(二件效果激活)
    二件效果 --> 四件效果: 激活4件(四件效果激活)
    四件效果 --> 六件效果: 激活6件(六件效果激活)

    六件效果 --> 四件效果: 取消激活(剩余4件)
    四件效果 --> 二件效果: 取消激活(剩余2件)
    二件效果 --> 单件激活: 取消激活(剩余1件)
    单件激活 --> 未收集: 取消所有激活

    六件效果 --> [*]: 套装完成
```

---

## 十三、系统模块依赖关系

### 13.1 模块依赖图

```mermaid
flowchart LR
    subgraph 博物馆模块
        A[博物馆核心]
        B[藏品管理]
        C[展位管理]
        D[套装系统]
        E[属性计算]
    end

    subgraph 玩家模块
        F[玩家信息]
        G[等级系统]
        H[VIP系统]
    end

    subgraph 资源模块
        I[背包系统]
        J[货币系统]
        K[道具系统]
    end

    subgraph 任务模块
        L[任务系统]
        M[成就系统]
    end

    subgraph 活动模块
        N[活动系统]
        O[副本系统]
    end

    A --> F
    B --> I
    B --> J
    C --> K
    D --> B
    E --> F

    B --> L
    B --> M
    A --> N
    A --> O

    F -.-> A
    I -.-> B
    L -.-> B
```

### 13.2 数据流向图

```mermaid
flowchart TD
    subgraph 数据来源
        A1[配置数据]
        A2[玩家数据]
        A3[运行时数据]
    end

    subgraph 处理层
        B1[配置加载器]
        B2[数据管理器]
        B3[业务逻辑层]
    end

    subgraph 存储层
        C1[(MySQL)]
        C2[(Redis)]
        C3[本地缓存]
    end

    subgraph 输出层
        D1[客户端推送]
        D2[日志记录]
        D3[统计分析]
    end

    A1 --> B1
    A2 --> B2
    A3 --> B3

    B1 --> C3
    B2 --> C1
    B2 --> C2
    B3 --> C1
    B3 --> C2

    B3 --> D1
    B3 --> D2
    B3 --> D3
```

---

## 十四、常见业务场景示例

### 14.1 新玩家首次获取藏品

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Dungeon as 副本系统
    participant Museum as 博物馆模块
    participant Bag as 背包模块
    participant Push as 推送系统

    Player->>Dungeon: 完成新手副本
    Dungeon->>Museum: 触发藏品掉落(itemId=10001)
    Museum->>Museum: 检查玩家博物馆状态

    alt 博物馆未初始化
        Museum->>Museum: 创建博物馆数据
        Museum->>Museum: 初始化展位数据
    end

    Museum->>Museum: 检查是否已拥有藏品

    alt 已拥有
        Museum->>Bag: 增加碎片
        Museum->>Push: 推送OnMuseumItemChg
    else 未拥有
        Museum->>Museum: 创建藏品记录
        Museum->>Museum: 设置初始等级=1
        Museum->>Museum: 设置激活状态=未激活
        Museum->>Museum: 检查套装归属
        Museum->>Push: 推送OnMuseumItemAdd
    end

    Museum->>Museum: 更新收集进度
    Museum->>Museum: 触发成就检测
    Push-->>Player: 显示获得提示
```

### 14.2 玩家升级藏品触发套装效果

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Client as 客户端
    participant Museum as 博物馆服务
    participant Set as 套装服务
    participant Attr as 属性服务
    participant Push as 推送服务

    Player->>Client: 点击升级按钮
    Client->>Museum: ReqMuseumItemUpgrade(itemId, count)

    Museum->>Museum: 验证升级条件
    Museum->>Museum: 扣除升级材料
    Museum->>Museum: 执行升级判定

    alt 升级成功
        Museum->>Museum: 等级+1
        Museum->>Museum: 更新属性

        alt 藏品已激活
            Museum->>Attr: 重新计算总属性
            Attr-->>Museum: 返回属性差值

            alt 达到激活等级
                Museum->>Set: 检查套装激活数
                Set->>Set: 增加激活数
                Set->>Set: 计算套装效果

                alt 新增套装效果激活
                    Set->>Push: 推送OnMuseumSetEffect
                    Set->>Attr: 应用套装效果
                end
            end

            Museum->>Push: 推送OnMuseumAttrChg
        end

        Museum->>Push: 推送OnMuseumItemChg(成功)
    else 升级失败
        Museum->>Push: 推送OnMuseumItemChg(失败)
    end

    Push-->>Client: 更新UI显示
    Client-->>Player: 显示升级结果
```

---

*文档生成时间: 2026-04-12*
*文档版本: v2.2*
