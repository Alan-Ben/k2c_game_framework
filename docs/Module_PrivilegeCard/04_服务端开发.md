# 权益卡服务端开发文档

## 一、模块架构

### 1.1 模块概述

权益卡模块（PrivilegeCard Module）负责管理玩家的权益卡数据，包括：
- 权益卡激活与过期管理
- 每日奖励发放
- 属性加成计算
- 自动结算收益

### 1.2 组件类型

组件类型: `ENPPlayerCompType.PRIVILEGE_CARD`

---

## 二、核心数据结构

### 2.1 权益卡组件 (PrivilegeCardComponent)

**路径**: `NPUSServer/NPUSUserMgr/UserComp/PrivilegeCardComp/PrivilegeCardComponent.java`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| _m_arrPrivilegeCardArr | PrivilegeCardInfo[] | 权益卡数据数组 |
| _m_pcPlayerPropertyContainer | NPPlayerPropertyContainer | 玩家属性容器 |

### 2.2 权益卡信息 (PrivilegeCardInfo)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| cardType | EPrivilegeCardType | 权益卡类型 |
| expireTimeS | int | 过期时间戳 |
| lastRewardTimeS | int | 上次领奖时间 |

---

## 三、物品接口实现

### 3.1 物品类型

权益卡实现 `_IUserItemBasicDealer` 和 `_ITickableComponent` 接口：

```java
public class PrivilegeCardComponent extends _ANPUserComponent
    implements _IUserItemBasicDealer, _ITickableComponent, _IHandlerHolder
```

### 3.2 定时检查

```java
@Override
public void tick1Sec()
{
    getUserData().lockUser();
    try
    {
        for (PrivilegeCardInfo info : _m_arrPrivilegeCardArr)
        {
            if (info == null) continue;
            info.checkInvalid();  // 检查权益卡失效情况
        }
    }
    finally
    {
        getUserData().unlockUser();
    }
}
```

---

## 四、核心逻辑

### 4.1 获得权益卡

```java
public void gainCard(EPrivilegeCardType _type, long _count, NPPlayerContext _context)
{
    getUserData().lockUser();
    try
    {
        PrivilegeCardInfo info = getPrivilegeCard(_type);
        if (info == null || info.getRef() == null) return;

        // 增加次数
        info._addCount(_count, _context);

        // 检查生效
        info.checkEffective(true);

        // 推送数据
        getUserData().sendMsgToGC(US2GCWriter_021_PlayerInfo.make_054_OnPrivilegeCardChg(info));
    }
    finally
    {
        getUserData().unlockUser();
    }
}
```

### 4.2 自动结算

```java
public void autoSettleAll()
{
    for (PrivilegeCardInfo info : _m_arrPrivilegeCardArr)
    {
        if (info == null) continue;
        info.autoSettle(NPPlayerContext.createNew(ENPGameEvent.PRIVILEGE_CARD_AUTO_SETTLE), null);
    }

    // 注册跨天事件监听器
    getUserData().OnCrossDay.addHandler(this, new HandlerOne<Integer>()
    {
        @Override
        public void handle(Integer _nowTag)
        {
            for (PrivilegeCardInfo info : _m_arrPrivilegeCardArr)
            {
                if (info == null) continue;
                info.autoSettle(NPPlayerContext.createNew(ENPGameEvent.SERVER_CROSS_DAY), null);
            }
        }
    });
}
```

---

## 五、数据库设计

### 5.1 权益卡数据表

**表名**: `player_privilege_card`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键 |
| cid | BIGINT | 玩家CID |
| card_type | INT | 权益卡类型 |
| expire_time_s | INT | 过期时间戳 |
| last_reward_time_s | INT | 上次领奖时间 |
| total_gain_days | INT | 累计获得天数 |

---

## 六、协议构造

### 6.1 构造数据协议

```java
public void makeProto(ArrayList<PrivilegeCardObj_Info> _list)
{
    getUserData().lockUser();
    try
    {
        for (PrivilegeCardInfo info : _m_arrPrivilegeCardArr)
        {
            if (info == null) continue;
            _list.add(info.toProto());
        }
    }
    finally
    {
        getUserData().unlockUser();
    }
}
```

---

*文档生成时间: 2026-04-06*
