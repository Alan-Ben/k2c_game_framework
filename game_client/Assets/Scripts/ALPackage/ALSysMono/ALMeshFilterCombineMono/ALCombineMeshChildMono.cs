using UnityEngine;

namespace ALPackage
{
    /****************
     * Combine操作的子操作对象
     **/
    public class ALCombineMeshChildMono : MonoBehaviour
    {
        /** Mesh编号，用于与父对象核对 */
        public int timeTag;
        public int combineMeshID = 0;

        // Use this for initialization
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}
