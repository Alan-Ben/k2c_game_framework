JavaPackage GOM2CD_R.gom_p001_DataOp;

/**********************************************************
 * 请求删除某个数据的协议
 **********************************************************/
ALProtocol GOM2CD_R_001_003_RmvData <1, 3>
{
	int dataType[数据类型];
	long groupId[分组Id];
	int usId[US服务器Id];
	
	long[] dataId[数据Id标识，这个根据每个数据格式不同各自确认];
}
